

Tested on python 3.6.6
```
Usage: e-prime-reader.py [options]

Options:
  -h, --help            show this help message and exit
  -d CSV_FILE, --data=CSV_FILE
                        Csv file generated by e-prime
  -o OUT_DIRECTORY, --out-dir=OUT_DIRECTORY
                        output directory
  -s SESSION, --session=SESSION
                        session number
  --name=NAME           Test name (will be included in output filenames)

```